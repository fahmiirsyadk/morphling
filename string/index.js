const uppercase = require("./uppercase");
const lowercase = require("./lowercase");
const replace = require("./replace");
const join = require("./join");

module.exports = { uppercase, lowercase, replace, join };
