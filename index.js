const { pipe, compose } = require("./core");
const { map, filter, uniq } = require("./array");
const { uppercase, lowercase, join, replace } = require("./string");

module.exports = {
  pipe,
  compose,
  map,
  filter,
  uniq,
  uppercase,
  lowercase,
  join,
  replace
};
